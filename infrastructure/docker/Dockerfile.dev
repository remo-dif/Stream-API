FROM node:20-alpine

WORKDIR /app

# Install all dependencies because the development server needs Nest CLI and test tooling.
COPY package*.json ./
RUN npm ci

COPY . .

ENV NODE_ENV=development
EXPOSE 3000

# Hot reload is enabled by the compose volume that mounts src/ into this container.
CMD ["npm", "run", "start:dev"]
